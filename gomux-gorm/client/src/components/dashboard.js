import { Card, CardContent, CardHeader } from "@material-ui/core";
import React from "react";

const Dashboard = () => {
  return (
    <Card>
      <CardHeader title="Welcom to GEN Indonesia." />
      <CardContent>Gorilla Mux + Gorm + ReactAdmin</CardContent>
    </Card>
  );
};

export default Dashboard;
